version: '3.9'
services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DB_PATH: /data/database.db
    volumes:
      - ./uploads:/app/uploads
      - ./data:/data
