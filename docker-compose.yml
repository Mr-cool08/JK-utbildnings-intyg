version: '3.9'
services:
  app:
    build: .
    ports:
      - "80:80"
    env_file:
      - .env
    environment:
      DB_PATH: /data/database.db
    volumes:
      - ./uploads:/app/uploads
      - ./data:/data
