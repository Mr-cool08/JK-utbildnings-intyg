version: '3.9'
services:
  app:
    build: .
    ports:
      - "80:80"
      - "443:443"
    user: "101:102"
    env_file:
      - .env
    environment:
      DATABASE_URL: "${DATABASE_URL:?Set DATABASE_URL in .env to point at your Postgres instance}"
